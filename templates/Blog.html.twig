<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="../public/assets/css/MeP.css">
    <script src="../public/assets/js/scriptNav.js"></script>
    <title>Blog</title>
</head>
<body>
<!-- Header -->
{% include 'Header.html.twig' %}
<h3>Les Recettes du Blog Culinaire proposées par votre chef Louis Lamotte</h3>
<div class="articles">
    {% for a in articles %}
    <article>
        <h2>{{ a.Name }}</h2>
        <p>{{ a.Date|date('d/m/y') }}</p>
        <img src={{ asset('../public/assets/img/recette2.jpg') }} alt="">
        <p>{{ a.Description }}</p>
    </article>
    {% endfor %}
    <input type="text" class="admin">
    <button hidden><a href={{ path('newArticle') }}>LOL</a></button>
</div>

<!-- Footer -->
{% include 'Footer.html.twig' %}
</body>
</html>