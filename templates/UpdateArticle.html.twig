<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href={{ asset("assets/css/MeP.css") }}>

    <title>Modifier une recette</title>
</head>
<body>
<!-- Header -->
{% include 'Header.html.twig' %}
<main class="wrap">
{% block content %}

    <h2>Modification d'un article</h2>

    {{ form_start(formulaire) }}

    <div>
        {{ form_label(formulaire.Name,"Intitulé de la recette") }}
        {{ form_widget(formulaire.Name) }}
    </div>

    <div>
        {{ form_label(formulaire.Description,"Le contenu de l'article") }}
        {{ form_widget(formulaire.Description) }}
    </div>

    {{ form_end(formulaire) }}

{% endblock %}
</main>
<!-- Footer -->
{% include 'Footer.html.twig' %}

</body>
</html>